services:
  backend:
    build: ./backend
    ports:
      - "8080:8080"
    environment:
      - ALLOWED_ORIGIN=http://localhost:5173

  frontend:
    build: ./frontend
    ports:
      - "5173:80"
    depends_on:
      - backend
