# Frontend

React + TypeScript app bootstrapped for Vite.

## Run locally:

```bash
cd frontend
npm install
npm run dev
```
## Run the tests
```bash
npm run test
```

or to get code coverage:
```bash
npx vitest run --coverage
```